#include <stdio.h>
float main()
{
printf("Gitonga's factory\n");
printf("Employee number\n");
printf("KRA Pin\n");
scanf("%d",&KRA Pin);
KRA Pin = 30/100 * total income;
printf("KRA Pin=%d",&KRA Pin);
printf("Number of hours worked\n");
scanf("%d",&hours worked);
if(hours worked = 40hours)
{
    printf("1000 ksh");
}
else
    if(hours worked > 40hours)
{
    printf("1500");
}
normal pay = regular pay rate*40hours;
   printf("normal pay=%d",&normal pay);
overtime pay = regular pay rate 1.5 * 40hours;
   printf("overtime pay=%d",&overtime pay);
tax paid = 30/100*total income;
   printf("tax paid=%d", &tax paid);
net income = total income - (30/100 * total income)
   printf("net income=%d", &net icome);
return 0;
}
